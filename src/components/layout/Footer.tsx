export function Footer() {
  return (
    <footer className="bg-white border-t border-border px-4 lg:px-6 py-2.5">
      <p className="text-[11px] text-muted-foreground text-center">
        Executed via regulated partners. MULTIFLY does not hold funds, custody assets, or execute transactions.
      </p>
    </footer>
  );
}
